apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-oracle
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/u01/app/oracle/oradata/ORCL" 
